name: CI/CD Pipeline 2

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: runner-2
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set Up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.9"

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Deploy App (Simulation)
        run: echo "Deploying Flask App from develop branch!"
